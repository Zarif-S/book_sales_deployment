# In your next script, you can access residuals from the trained model
results = book_sales_arima_pipeline(...)
trained_model = results["trained_model"]

# Extract residuals
residuals = trained_model.resid

# Save residuals if needed
residuals_df = pd.DataFrame({
    'date': residuals.index,
    'residuals': residuals.values
})
residuals_df.to_csv('arima_residuals.csv', index=False)